function init(self)
	msg.post(".", "acquire_input_focus") 
end

function on_input(self, action_id, action)
	if action_id == hash("click") and action.pressed then 
		local button = gui.get_node("play-button")
		if gui.pick_node(button, action.x, action.y) then 
			print("Hello!")
		end
	end
end
